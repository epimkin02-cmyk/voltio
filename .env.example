# Public origin of the deployed site, no trailing slash (canonical URLs, OG tags, sitemap).
NEXT_PUBLIC_SITE_URL=
# Optional webhook / CRM endpoint the lead form is forwarded to (server-only).
CONTACT_ENDPOINT=
